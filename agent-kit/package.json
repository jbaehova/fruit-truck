{
  "name": "@fruit-truck/agent-kit",
  "version": "2.1.0",
  "description": "Standalone MCP server and Agent Skills for Fruit Truck",
  "type": "module",
  "bin": {
    "fruit-truck-mcp": "dist/scripts/mcp-server.js",
    "fruit-truck-agent-kit": "bin/install.mjs"
  },
  "files": [
    "bin",
    "compatibility.json",
    "dist",
    "mcp.example.json",
    "skills",
    "README.md"
  ],
  "scripts": {
    "build": "node scripts/build-package.mjs",
    "check": "node scripts/check-compatibility.mjs",
    "prepack": "npm run build && npm run check"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
